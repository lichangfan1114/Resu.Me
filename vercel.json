{
  "version": 2,
  "builds": [
    {
      "src": "resumebuilder-ai/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "resumebuilder-ai/$1" }
  ]
}
